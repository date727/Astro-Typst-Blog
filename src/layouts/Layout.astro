<!doctype html>
<html lang="zh-cn">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title><slot name="title" /></title>

		<!-- 1. 预加载字体（优先加载关键字体文件） -->
		<link 
			rel="preload" 
			href="/fonts/consola-1.woff2"
			as="font" 
			type="font/woff2" 
			crossorigin 
		/>

		<style>
			/* 2. 声明自定义字体，关联文件与名称 */
			@font-face {
				font-family: 'MyConsola'; /* 自定义名称，避免与系统字体冲突 */
				src: url('/fonts/consola-1.woff2') format('woff2'),  /* 现代浏览器优先 */
					 url('/fonts/consola-1.ttf') format('truetype'); /* 降级格式 */
				font-weight: normal; /* 根据实际字体设置 */
				font-style: normal;
			}

			body {
				margin: 0;
				background-color: #fafaf0;
				font-family: 'MyConsola', Consolas, monospace; /* 优先级：自定义字体 > 系统Consolas > 通用等宽字体 */
			}

			nav {
				padding: 1.5rem 1rem;
				max-width: 800px;
				margin: 0 auto;
				text-align: right;
				border-bottom: 1px solid rgb(0, 128, 128);
			}
			nav a {
				margin-right: 1rem;
				text-decoration: none;
				font-weight: bold;
				color: #333;
			}
			main {
				padding: 2rem;
				max-width: 800px;
				margin: 0 auto;
			}
		</style>
	</head>
	<body>
		<nav>
			<a href="/">Home</a>
			<a href="/about/">About</a>
			<a href="/blog/">Blog</a>
			<a href="/comments">Comment</a>
		</nav>
		<main>
			<slot />
		</main>
	</body>
</html>